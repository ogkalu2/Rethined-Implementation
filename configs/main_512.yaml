# Main Training Run, 512x512 input
model:
  coarse_model:
    class: "CoarseModel"
    channels: [64, 128, 256, 384, 512]
    head_channels: 32
    use_rep_blocks: true
  generator:
    kernel_size: 8
    value_patch_size: 8
    value_source: "rgb"
    attention_temperature: 0.5
    attention_top_k: 32
    attention_supervision_top_k: 128
    attention_selection: "gumbel"
    attention_gumbel_tau: 1.0
    matching_descriptor_dim: 256
    match_coarse_rgb: true
    nheads: 1
    stem_out_stride: 1
    stem_out_channels: 3
    image_size: 512
    embed_dim: 2048
    use_positional_encoding: true
    dropout: 0.1
    feature_i: 3
    feature_dim: null
    concat_features: true
    detach_coarse_rgb: false
    final_conv: true
    attention_masking: true

data:
  root_dir: "."
  manifest_path: "dataset/manifests/main_dataset_manifest.csv"
  val_dir: null
  image_size: 1024
  batch_size: 4
  eval_batch_size: 2
  num_workers: 2
  mask_min_coverage: 0.10
  mask_max_coverage: 0.35
  force_random_masks_train: true
  force_random_masks_eval: true

training:
  total_steps: 178218
  grad_accum_steps: 4
  lr: 0.001
  discriminator_lr: 0.00002
  warmup_steps: 3000
  min_lr: 0.000001
  discriminator_min_lr: 0.0000001
  grad_clip: 1.0
  discriminator_grad_clip: 1.0
  mixed_precision: true
  seed: 42
  betas: [0.9, 0.999]

loss:
  coarse_l2_weight: 1.0
  coarse_blur_l1_weight: 0.5
  coarse_gradient_weight: 0.1
  coarse_perceptual_weight: 0.1
  refined_l1_weight: 1.0
  frequency_weight: 0.5
  perceptual_weight: 0.1
  adversarial_weight: 0.02
  attention_supervision_weight: 0.05
  attention_supervision_temperature: 0.15
  attention_supervision_hard_targets: true
  attention_supervision_confidence_threshold: 0.12
  attention_supervision_margin_threshold: 0.02
  attention_supervision_start_step: 3000
  attention_supervision_full_step: 12000
  attention_coherence_weight: 0.01
  attention_coherence_similarity_threshold: 0.55
  attention_coherence_start_step: 9000
  attention_coherence_full_step: 24000
  adversarial_mode: "hinge"
  focal_alpha: 1.0
  focal_log_matrix: false

discriminator:
  in_channels: 3
  base_channels: 64
  n_layers: 3
  num_scales: 1 

logging:
  log_dir: "logs/train_main"
  log_interval: 50
  vis_interval: 1000
  eval_interval: 1000
  eval_batches: 16
  save_interval: 5000
  save_checkpoints: true
  save_best_checkpoint: true
  save_best_metric: "masked_l1_hr_refined"
  save_best_mode: "min"
  save_final_checkpoint: true
  checkpoint_steps: [50000, 100000, 178218]
  keep_last_checkpoints: 3
  print_train_metrics: false
