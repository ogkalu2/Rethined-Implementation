# Dataset Workspace

This folder documents the intended on-disk layout for a reproducible DF8K
surrogate dataset used by V6 training.

The actual image data should live under `datasets/df8k_surrogate/` at the repo
root as described in `v6/docs/DF8K_SURROGATE_DATASET.md`.
